{
    "version":2,
    "build":[{
    "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "collection" }
    }],
    "routes":[{
        "src":"/(.*)",
        "dest":"/app.js"
    }]
}