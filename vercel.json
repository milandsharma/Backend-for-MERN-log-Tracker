{
    "version":2,
    "builds":[{
    "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "collection" }
    }],
    "routes":[{
        "src":"/(.*)",
        "dest":"/app.js"
    }]
}